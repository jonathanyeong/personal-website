---
const { title, description, slug } = Astro.props;
---

<div class="card">
  <h3 class="card-title">{title}</h3>
  <p class="card-description">{description}</p>
  <a href={`/${slug}`}>Read More</a>
</div>

<style>
  .card {
    background: #F2FCFA;
    border-left: 12px solid #AFE8E7;
    border-radius: 5px;
    padding: 32px 30px;
  }

  .card-title {
    font-size: 22px;
    font-weight: bold;
    margin-bottom: 12px;
  }

  .card-description {
    margin-bottom: 60px;
  }
</style>
